////////////////////////////////////////////////////////////////////////////////
// Settings
////////////////////////////////////////////////////////////////////////////////

'use strict'

module.exports = function(variables){

	var cssString = Object.keys(variables).map(key => {
		return `$${key}: "${variables[key]}";`
	}).join('\n')



}
